"""
core.repositories — Repository layer package.

This package intentionally avoids importing submodules at initialization.
Clients should import repository classes directly from individual modules.
"""
